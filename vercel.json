
{
  "buildCommand": "git config --global user.name 'ProjectHub Developer' && git config --global user.email 'developer@projecthub.dev' && npm run build",
  "outputDirectory": "dist/public",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
